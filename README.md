# Al Hafsa — Vercel Ready

This version avoids the Framer Motion/Webpack compatibility error shown in the Vercel build log.

## Local
npm install
npm run build
npm run dev

## Vercel
Push these files to GitHub and redeploy the project. Vercel will run `npm run build` automatically.
